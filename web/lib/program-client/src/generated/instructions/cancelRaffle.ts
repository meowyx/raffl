/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { RAFFL_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_RAFFLE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  135, 191, 223, 141, 192, 186, 234, 254,
]);

export function getCancelRaffleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_RAFFLE_DISCRIMINATOR,
  );
}

export type CancelRaffleInstruction<
  TProgram extends string = typeof RAFFL_PROGRAM_ADDRESS,
  TAccountInitiator extends string | AccountMeta<string> = string,
  TAccountRaffle extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountInitiator extends string
        ? ReadonlySignerAccount<TAccountInitiator> &
            AccountSignerMeta<TAccountInitiator>
        : TAccountInitiator,
      TAccountRaffle extends string
        ? WritableAccount<TAccountRaffle>
        : TAccountRaffle,
      ...TRemainingAccounts,
    ]
  >;

export type CancelRaffleInstructionData = { discriminator: ReadonlyUint8Array };

export type CancelRaffleInstructionDataArgs = {};

export function getCancelRaffleInstructionDataEncoder(): FixedSizeEncoder<CancelRaffleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CANCEL_RAFFLE_DISCRIMINATOR }),
  );
}

export function getCancelRaffleInstructionDataDecoder(): FixedSizeDecoder<CancelRaffleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelRaffleInstructionDataCodec(): FixedSizeCodec<
  CancelRaffleInstructionDataArgs,
  CancelRaffleInstructionData
> {
  return combineCodec(
    getCancelRaffleInstructionDataEncoder(),
    getCancelRaffleInstructionDataDecoder(),
  );
}

export type CancelRaffleInput<
  TAccountInitiator extends string = string,
  TAccountRaffle extends string = string,
> = {
  initiator: TransactionSigner<TAccountInitiator>;
  raffle: Address<TAccountRaffle>;
};

export function getCancelRaffleInstruction<
  TAccountInitiator extends string,
  TAccountRaffle extends string,
  TProgramAddress extends Address = typeof RAFFL_PROGRAM_ADDRESS,
>(
  input: CancelRaffleInput<TAccountInitiator, TAccountRaffle>,
  config?: { programAddress?: TProgramAddress },
): CancelRaffleInstruction<TProgramAddress, TAccountInitiator, TAccountRaffle> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    initiator: { value: input.initiator ?? null, isWritable: false },
    raffle: { value: input.raffle ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("initiator", accounts.initiator),
      getAccountMeta("raffle", accounts.raffle),
    ],
    data: getCancelRaffleInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelRaffleInstruction<
    TProgramAddress,
    TAccountInitiator,
    TAccountRaffle
  >);
}

export type ParsedCancelRaffleInstruction<
  TProgram extends string = typeof RAFFL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    initiator: TAccountMetas[0];
    raffle: TAccountMetas[1];
  };
  data: CancelRaffleInstructionData;
};

export function parseCancelRaffleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelRaffleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { initiator: getNextAccount(), raffle: getNextAccount() },
    data: getCancelRaffleInstructionDataDecoder().decode(instruction.data),
  };
}
