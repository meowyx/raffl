/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { RAFFL_PROGRAM_ADDRESS } from "../programs";

export const SETTLE_RAFFLE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  136, 98, 44, 133, 40, 36, 190, 185,
]);

export function getSettleRaffleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SETTLE_RAFFLE_DISCRIMINATOR,
  );
}

export type SettleRaffleInstruction<
  TProgram extends string = typeof RAFFL_PROGRAM_ADDRESS,
  TAccountInitiator extends string | AccountMeta<string> = string,
  TAccountRaffle extends string | AccountMeta<string> = string,
  TAccountRandomnessAccountData extends string | AccountMeta<string> = string,
  TAccountTicket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountInitiator extends string
        ? ReadonlySignerAccount<TAccountInitiator> &
            AccountSignerMeta<TAccountInitiator>
        : TAccountInitiator,
      TAccountRaffle extends string
        ? WritableAccount<TAccountRaffle>
        : TAccountRaffle,
      TAccountRandomnessAccountData extends string
        ? ReadonlyAccount<TAccountRandomnessAccountData>
        : TAccountRandomnessAccountData,
      TAccountTicket extends string
        ? ReadonlyAccount<TAccountTicket>
        : TAccountTicket,
      ...TRemainingAccounts,
    ]
  >;

export type SettleRaffleInstructionData = { discriminator: ReadonlyUint8Array };

export type SettleRaffleInstructionDataArgs = {};

export function getSettleRaffleInstructionDataEncoder(): FixedSizeEncoder<SettleRaffleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SETTLE_RAFFLE_DISCRIMINATOR }),
  );
}

export function getSettleRaffleInstructionDataDecoder(): FixedSizeDecoder<SettleRaffleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSettleRaffleInstructionDataCodec(): FixedSizeCodec<
  SettleRaffleInstructionDataArgs,
  SettleRaffleInstructionData
> {
  return combineCodec(
    getSettleRaffleInstructionDataEncoder(),
    getSettleRaffleInstructionDataDecoder(),
  );
}

export type SettleRaffleInput<
  TAccountInitiator extends string = string,
  TAccountRaffle extends string = string,
  TAccountRandomnessAccountData extends string = string,
  TAccountTicket extends string = string,
> = {
  initiator: TransactionSigner<TAccountInitiator>;
  raffle: Address<TAccountRaffle>;
  /**
   * the Switchboard program, key matches the one stored at request_draw,
   * seed_slot matches commit_slot, and reveal_slot == current slot.
   */
  randomnessAccountData: Address<TAccountRandomnessAccountData>;
  /**
   * The Ticket PDA at the derived winning index. Not initialized here —
   * just read for ownership and `buyer` lookup. We re-derive seeds to
   * confirm the caller can't substitute a different ticket.
   */
  ticket: Address<TAccountTicket>;
};

export function getSettleRaffleInstruction<
  TAccountInitiator extends string,
  TAccountRaffle extends string,
  TAccountRandomnessAccountData extends string,
  TAccountTicket extends string,
  TProgramAddress extends Address = typeof RAFFL_PROGRAM_ADDRESS,
>(
  input: SettleRaffleInput<
    TAccountInitiator,
    TAccountRaffle,
    TAccountRandomnessAccountData,
    TAccountTicket
  >,
  config?: { programAddress?: TProgramAddress },
): SettleRaffleInstruction<
  TProgramAddress,
  TAccountInitiator,
  TAccountRaffle,
  TAccountRandomnessAccountData,
  TAccountTicket
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    initiator: { value: input.initiator ?? null, isWritable: false },
    raffle: { value: input.raffle ?? null, isWritable: true },
    randomnessAccountData: {
      value: input.randomnessAccountData ?? null,
      isWritable: false,
    },
    ticket: { value: input.ticket ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("initiator", accounts.initiator),
      getAccountMeta("raffle", accounts.raffle),
      getAccountMeta("randomnessAccountData", accounts.randomnessAccountData),
      getAccountMeta("ticket", accounts.ticket),
    ],
    data: getSettleRaffleInstructionDataEncoder().encode({}),
    programAddress,
  } as SettleRaffleInstruction<
    TProgramAddress,
    TAccountInitiator,
    TAccountRaffle,
    TAccountRandomnessAccountData,
    TAccountTicket
  >);
}

export type ParsedSettleRaffleInstruction<
  TProgram extends string = typeof RAFFL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    initiator: TAccountMetas[0];
    raffle: TAccountMetas[1];
    /**
     * the Switchboard program, key matches the one stored at request_draw,
     * seed_slot matches commit_slot, and reveal_slot == current slot.
     */
    randomnessAccountData: TAccountMetas[2];
    /**
     * The Ticket PDA at the derived winning index. Not initialized here —
     * just read for ownership and `buyer` lookup. We re-derive seeds to
     * confirm the caller can't substitute a different ticket.
     */
    ticket: TAccountMetas[3];
  };
  data: SettleRaffleInstructionData;
};

export function parseSettleRaffleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettleRaffleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      initiator: getNextAccount(),
      raffle: getNextAccount(),
      randomnessAccountData: getNextAccount(),
      ticket: getNextAccount(),
    },
    data: getSettleRaffleInstructionDataDecoder().decode(instruction.data),
  };
}
