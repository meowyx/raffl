/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findPlatformPda, findRafflePda, findVaultPda } from "../pdas";
import { RAFFL_PROGRAM_ADDRESS } from "../programs";
import {
  getPrizeTypeDecoder,
  getPrizeTypeEncoder,
  type PrizeType,
  type PrizeTypeArgs,
} from "../types";

export const CREATE_RAFFLE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  226, 206, 159, 34, 213, 207, 98, 126,
]);

export function getCreateRaffleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_RAFFLE_DISCRIMINATOR,
  );
}

export type CreateRaffleInstruction<
  TProgram extends string = typeof RAFFL_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TAccountRaffle extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountPlatform extends string
        ? WritableAccount<TAccountPlatform>
        : TAccountPlatform,
      TAccountRaffle extends string
        ? WritableAccount<TAccountRaffle>
        : TAccountRaffle,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateRaffleInstructionData = {
  discriminator: ReadonlyUint8Array;
  nonce: bigint;
  prizeType: PrizeType;
  prizeAmount: bigint;
  prizeDescription: string;
  ticketPrice: bigint;
  maxTickets: number;
  minTickets: number;
  endTime: bigint;
};

export type CreateRaffleInstructionDataArgs = {
  nonce: number | bigint;
  prizeType: PrizeTypeArgs;
  prizeAmount: number | bigint;
  prizeDescription: string;
  ticketPrice: number | bigint;
  maxTickets: number;
  minTickets: number;
  endTime: number | bigint;
};

export function getCreateRaffleInstructionDataEncoder(): Encoder<CreateRaffleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nonce", getU64Encoder()],
      ["prizeType", getPrizeTypeEncoder()],
      ["prizeAmount", getU64Encoder()],
      [
        "prizeDescription",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      ["ticketPrice", getU64Encoder()],
      ["maxTickets", getU32Encoder()],
      ["minTickets", getU32Encoder()],
      ["endTime", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_RAFFLE_DISCRIMINATOR }),
  );
}

export function getCreateRaffleInstructionDataDecoder(): Decoder<CreateRaffleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nonce", getU64Decoder()],
    ["prizeType", getPrizeTypeDecoder()],
    ["prizeAmount", getU64Decoder()],
    [
      "prizeDescription",
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
    ["ticketPrice", getU64Decoder()],
    ["maxTickets", getU32Decoder()],
    ["minTickets", getU32Decoder()],
    ["endTime", getI64Decoder()],
  ]);
}

export function getCreateRaffleInstructionDataCodec(): Codec<
  CreateRaffleInstructionDataArgs,
  CreateRaffleInstructionData
> {
  return combineCodec(
    getCreateRaffleInstructionDataEncoder(),
    getCreateRaffleInstructionDataDecoder(),
  );
}

export type CreateRaffleAsyncInput<
  TAccountCreator extends string = string,
  TAccountPlatform extends string = string,
  TAccountRaffle extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  platform?: Address<TAccountPlatform>;
  raffle?: Address<TAccountRaffle>;
  /**
   * are deposited via system_program::transfer below. Withdrawal paths
   * (settle/claim/cancel) re-derive with `bump = raffle.vault_bump`.
   */
  vault?: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  nonce: CreateRaffleInstructionDataArgs["nonce"];
  prizeType: CreateRaffleInstructionDataArgs["prizeType"];
  prizeAmount: CreateRaffleInstructionDataArgs["prizeAmount"];
  prizeDescription: CreateRaffleInstructionDataArgs["prizeDescription"];
  ticketPrice: CreateRaffleInstructionDataArgs["ticketPrice"];
  maxTickets: CreateRaffleInstructionDataArgs["maxTickets"];
  minTickets: CreateRaffleInstructionDataArgs["minTickets"];
  endTime: CreateRaffleInstructionDataArgs["endTime"];
};

export async function getCreateRaffleInstructionAsync<
  TAccountCreator extends string,
  TAccountPlatform extends string,
  TAccountRaffle extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFL_PROGRAM_ADDRESS,
>(
  input: CreateRaffleAsyncInput<
    TAccountCreator,
    TAccountPlatform,
    TAccountRaffle,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateRaffleInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountPlatform,
    TAccountRaffle,
    TAccountVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: true },
    raffle: { value: input.raffle ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.platform.value) {
    accounts.platform.value = await findPlatformPda();
  }
  if (!accounts.raffle.value) {
    accounts.raffle.value = await findRafflePda({
      creator: getAddressFromResolvedInstructionAccount(
        "creator",
        accounts.creator.value,
      ),
      nonce: getNonNullResolvedInstructionInput("nonce", args.nonce),
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      raffle: getAddressFromResolvedInstructionAccount(
        "raffle",
        accounts.raffle.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("platform", accounts.platform),
      getAccountMeta("raffle", accounts.raffle),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateRaffleInstructionDataEncoder().encode(
      args as CreateRaffleInstructionDataArgs,
    ),
    programAddress,
  } as CreateRaffleInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountPlatform,
    TAccountRaffle,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type CreateRaffleInput<
  TAccountCreator extends string = string,
  TAccountPlatform extends string = string,
  TAccountRaffle extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  platform: Address<TAccountPlatform>;
  raffle: Address<TAccountRaffle>;
  /**
   * are deposited via system_program::transfer below. Withdrawal paths
   * (settle/claim/cancel) re-derive with `bump = raffle.vault_bump`.
   */
  vault: Address<TAccountVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  nonce: CreateRaffleInstructionDataArgs["nonce"];
  prizeType: CreateRaffleInstructionDataArgs["prizeType"];
  prizeAmount: CreateRaffleInstructionDataArgs["prizeAmount"];
  prizeDescription: CreateRaffleInstructionDataArgs["prizeDescription"];
  ticketPrice: CreateRaffleInstructionDataArgs["ticketPrice"];
  maxTickets: CreateRaffleInstructionDataArgs["maxTickets"];
  minTickets: CreateRaffleInstructionDataArgs["minTickets"];
  endTime: CreateRaffleInstructionDataArgs["endTime"];
};

export function getCreateRaffleInstruction<
  TAccountCreator extends string,
  TAccountPlatform extends string,
  TAccountRaffle extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFL_PROGRAM_ADDRESS,
>(
  input: CreateRaffleInput<
    TAccountCreator,
    TAccountPlatform,
    TAccountRaffle,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateRaffleInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountPlatform,
  TAccountRaffle,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: true },
    raffle: { value: input.raffle ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("platform", accounts.platform),
      getAccountMeta("raffle", accounts.raffle),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateRaffleInstructionDataEncoder().encode(
      args as CreateRaffleInstructionDataArgs,
    ),
    programAddress,
  } as CreateRaffleInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountPlatform,
    TAccountRaffle,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedCreateRaffleInstruction<
  TProgram extends string = typeof RAFFL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    platform: TAccountMetas[1];
    raffle: TAccountMetas[2];
    /**
     * are deposited via system_program::transfer below. Withdrawal paths
     * (settle/claim/cancel) re-derive with `bump = raffle.vault_bump`.
     */
    vault: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateRaffleInstructionData;
};

export function parseCreateRaffleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateRaffleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      creator: getNextAccount(),
      platform: getNextAccount(),
      raffle: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateRaffleInstructionDataDecoder().decode(instruction.data),
  };
}
