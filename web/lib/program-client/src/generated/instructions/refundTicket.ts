/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findVaultPda } from "../pdas";
import { RAFFL_PROGRAM_ADDRESS } from "../programs";

export const REFUND_TICKET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  178, 97, 75, 218, 227, 28, 21, 73,
]);

export function getRefundTicketDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REFUND_TICKET_DISCRIMINATOR,
  );
}

export type RefundTicketInstruction<
  TProgram extends string = typeof RAFFL_PROGRAM_ADDRESS,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountRaffle extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountTicket extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBuyer extends string
        ? WritableSignerAccount<TAccountBuyer> &
            AccountSignerMeta<TAccountBuyer>
        : TAccountBuyer,
      TAccountRaffle extends string
        ? WritableAccount<TAccountRaffle>
        : TAccountRaffle,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountTicket extends string
        ? WritableAccount<TAccountTicket>
        : TAccountTicket,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RefundTicketInstructionData = { discriminator: ReadonlyUint8Array };

export type RefundTicketInstructionDataArgs = {};

export function getRefundTicketInstructionDataEncoder(): FixedSizeEncoder<RefundTicketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REFUND_TICKET_DISCRIMINATOR }),
  );
}

export function getRefundTicketInstructionDataDecoder(): FixedSizeDecoder<RefundTicketInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRefundTicketInstructionDataCodec(): FixedSizeCodec<
  RefundTicketInstructionDataArgs,
  RefundTicketInstructionData
> {
  return combineCodec(
    getRefundTicketInstructionDataEncoder(),
    getRefundTicketInstructionDataDecoder(),
  );
}

export type RefundTicketAsyncInput<
  TAccountBuyer extends string = string,
  TAccountRaffle extends string = string,
  TAccountVault extends string = string,
  TAccountTicket extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  buyer: TransactionSigner<TAccountBuyer>;
  raffle: Address<TAccountRaffle>;
  vault?: Address<TAccountVault>;
  ticket: Address<TAccountTicket>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getRefundTicketInstructionAsync<
  TAccountBuyer extends string,
  TAccountRaffle extends string,
  TAccountVault extends string,
  TAccountTicket extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFL_PROGRAM_ADDRESS,
>(
  input: RefundTicketAsyncInput<
    TAccountBuyer,
    TAccountRaffle,
    TAccountVault,
    TAccountTicket,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RefundTicketInstruction<
    TProgramAddress,
    TAccountBuyer,
    TAccountRaffle,
    TAccountVault,
    TAccountTicket,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    buyer: { value: input.buyer ?? null, isWritable: true },
    raffle: { value: input.raffle ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    ticket: { value: input.ticket ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      raffle: getAddressFromResolvedInstructionAccount(
        "raffle",
        accounts.raffle.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("raffle", accounts.raffle),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("ticket", accounts.ticket),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRefundTicketInstructionDataEncoder().encode({}),
    programAddress,
  } as RefundTicketInstruction<
    TProgramAddress,
    TAccountBuyer,
    TAccountRaffle,
    TAccountVault,
    TAccountTicket,
    TAccountSystemProgram
  >);
}

export type RefundTicketInput<
  TAccountBuyer extends string = string,
  TAccountRaffle extends string = string,
  TAccountVault extends string = string,
  TAccountTicket extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  buyer: TransactionSigner<TAccountBuyer>;
  raffle: Address<TAccountRaffle>;
  vault: Address<TAccountVault>;
  ticket: Address<TAccountTicket>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRefundTicketInstruction<
  TAccountBuyer extends string,
  TAccountRaffle extends string,
  TAccountVault extends string,
  TAccountTicket extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RAFFL_PROGRAM_ADDRESS,
>(
  input: RefundTicketInput<
    TAccountBuyer,
    TAccountRaffle,
    TAccountVault,
    TAccountTicket,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RefundTicketInstruction<
  TProgramAddress,
  TAccountBuyer,
  TAccountRaffle,
  TAccountVault,
  TAccountTicket,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    buyer: { value: input.buyer ?? null, isWritable: true },
    raffle: { value: input.raffle ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    ticket: { value: input.ticket ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("raffle", accounts.raffle),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("ticket", accounts.ticket),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRefundTicketInstructionDataEncoder().encode({}),
    programAddress,
  } as RefundTicketInstruction<
    TProgramAddress,
    TAccountBuyer,
    TAccountRaffle,
    TAccountVault,
    TAccountTicket,
    TAccountSystemProgram
  >);
}

export type ParsedRefundTicketInstruction<
  TProgram extends string = typeof RAFFL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    buyer: TAccountMetas[0];
    raffle: TAccountMetas[1];
    vault: TAccountMetas[2];
    ticket: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RefundTicketInstructionData;
};

export function parseRefundTicketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRefundTicketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      buyer: getNextAccount(),
      raffle: getNextAccount(),
      vault: getNextAccount(),
      ticket: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRefundTicketInstructionDataDecoder().decode(instruction.data),
  };
}
