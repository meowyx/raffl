/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { RAFFL_PROGRAM_ADDRESS } from "../programs";

export const REQUEST_DRAW_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  22, 180, 8, 81, 47, 21, 86, 159,
]);

export function getRequestDrawDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_DRAW_DISCRIMINATOR,
  );
}

export type RequestDrawInstruction<
  TProgram extends string = typeof RAFFL_PROGRAM_ADDRESS,
  TAccountInitiator extends string | AccountMeta<string> = string,
  TAccountRaffle extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountRandomnessAccountData extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountInitiator extends string
        ? ReadonlySignerAccount<TAccountInitiator> &
            AccountSignerMeta<TAccountInitiator>
        : TAccountInitiator,
      TAccountRaffle extends string
        ? WritableAccount<TAccountRaffle>
        : TAccountRaffle,
      TAccountCreator extends string
        ? ReadonlyAccount<TAccountCreator>
        : TAccountCreator,
      TAccountRandomnessAccountData extends string
        ? ReadonlyAccount<TAccountRandomnessAccountData>
        : TAccountRandomnessAccountData,
      ...TRemainingAccounts,
    ]
  >;

export type RequestDrawInstructionData = { discriminator: ReadonlyUint8Array };

export type RequestDrawInstructionDataArgs = {};

export function getRequestDrawInstructionDataEncoder(): FixedSizeEncoder<RequestDrawInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REQUEST_DRAW_DISCRIMINATOR }),
  );
}

export function getRequestDrawInstructionDataDecoder(): FixedSizeDecoder<RequestDrawInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRequestDrawInstructionDataCodec(): FixedSizeCodec<
  RequestDrawInstructionDataArgs,
  RequestDrawInstructionData
> {
  return combineCodec(
    getRequestDrawInstructionDataEncoder(),
    getRequestDrawInstructionDataDecoder(),
  );
}

export type RequestDrawInput<
  TAccountInitiator extends string = string,
  TAccountRaffle extends string = string,
  TAccountCreator extends string = string,
  TAccountRandomnessAccountData extends string = string,
> = {
  /**
   * Audit fix: must be the raffle creator. Permissionless `request_draw`
   * let any actor pin a randomness account to the raffle and either
   * (a) refuse to reveal — locking Drawing forever — or (b) bind the
   * same SB account to multiple raffles in one tx, so only one of them
   * could ever settle. Restricting to the creator removes both vectors:
   * a malicious creator can already grief their own raffle (everyone
   * loses), and the timeout-based `cancel_raffle` covers a creator who
   * goes offline.
   */
  initiator: TransactionSigner<TAccountInitiator>;
  raffle: Address<TAccountRaffle>;
  /**
   * on `raffle` above. Required because Anchor's `has_one` resolves field
   * names against accounts in this struct.
   */
  creator: Address<TAccountCreator>;
  /**
   * verify the owner is the Switchboard program and that the data parses
   * to the expected layout. No deserialize-by-anchor because we use the
   * crate without the `anchor` feature (Anchor 1.0 trait conflict).
   */
  randomnessAccountData: Address<TAccountRandomnessAccountData>;
};

export function getRequestDrawInstruction<
  TAccountInitiator extends string,
  TAccountRaffle extends string,
  TAccountCreator extends string,
  TAccountRandomnessAccountData extends string,
  TProgramAddress extends Address = typeof RAFFL_PROGRAM_ADDRESS,
>(
  input: RequestDrawInput<
    TAccountInitiator,
    TAccountRaffle,
    TAccountCreator,
    TAccountRandomnessAccountData
  >,
  config?: { programAddress?: TProgramAddress },
): RequestDrawInstruction<
  TProgramAddress,
  TAccountInitiator,
  TAccountRaffle,
  TAccountCreator,
  TAccountRandomnessAccountData
> {
  // Program address.
  const programAddress = config?.programAddress ?? RAFFL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    initiator: { value: input.initiator ?? null, isWritable: false },
    raffle: { value: input.raffle ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: false },
    randomnessAccountData: {
      value: input.randomnessAccountData ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("initiator", accounts.initiator),
      getAccountMeta("raffle", accounts.raffle),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("randomnessAccountData", accounts.randomnessAccountData),
    ],
    data: getRequestDrawInstructionDataEncoder().encode({}),
    programAddress,
  } as RequestDrawInstruction<
    TProgramAddress,
    TAccountInitiator,
    TAccountRaffle,
    TAccountCreator,
    TAccountRandomnessAccountData
  >);
}

export type ParsedRequestDrawInstruction<
  TProgram extends string = typeof RAFFL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Audit fix: must be the raffle creator. Permissionless `request_draw`
     * let any actor pin a randomness account to the raffle and either
     * (a) refuse to reveal — locking Drawing forever — or (b) bind the
     * same SB account to multiple raffles in one tx, so only one of them
     * could ever settle. Restricting to the creator removes both vectors:
     * a malicious creator can already grief their own raffle (everyone
     * loses), and the timeout-based `cancel_raffle` covers a creator who
     * goes offline.
     */
    initiator: TAccountMetas[0];
    raffle: TAccountMetas[1];
    /**
     * on `raffle` above. Required because Anchor's `has_one` resolves field
     * names against accounts in this struct.
     */
    creator: TAccountMetas[2];
    /**
     * verify the owner is the Switchboard program and that the data parses
     * to the expected layout. No deserialize-by-anchor because we use the
     * crate without the `anchor` feature (Anchor 1.0 trait conflict).
     */
    randomnessAccountData: TAccountMetas[3];
  };
  data: RequestDrawInstructionData;
};

export function parseRequestDrawInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestDrawInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      initiator: getNextAccount(),
      raffle: getNextAccount(),
      creator: getNextAccount(),
      randomnessAccountData: getNextAccount(),
    },
    data: getRequestDrawInstructionDataDecoder().decode(instruction.data),
  };
}
